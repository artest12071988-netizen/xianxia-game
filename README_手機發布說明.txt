修仙大逃殺｜手機網址試玩版 V1

發布後，用 iPhone Safari 開啟網址，再點「分享」→「加入主畫面」。

GitHub Pages：
1. 新建 repository。
2. 將壓縮檔解壓後的所有內容上傳到 repository 根目錄。
3. 到 Settings → Pages，選擇 main 分支根目錄發布。
4. 等網站網址產生後，用 Safari 開啟。

注意：
- 網站根目錄必須直接看到 index.html。
- PWA 必須透過 HTTPS 網址運作。
